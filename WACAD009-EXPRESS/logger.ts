/*import { NextFuunction, Request, Response } from 'express'
const fs = require('fs')

type LoggerType = 'complete' | 'simple'

function logger(type: LoggerType) {

    if (type === 'simple') {
        return async (req: Request, res: Response, next: NextFuunction) =>{
            await fs.writeFile (`${ProcessingInstruction.cwd()}/${LOGS_PATH}/logs.log, 'cr`)
                flag: 'a'
            }
            
        }
    }
}*/